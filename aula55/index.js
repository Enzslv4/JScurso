// Funções imediatas = IIFE: Immediately Invoked Function Expression

// Basicamente a estrutura é: (function() {})()
// Isso cria uma função anonima
(function() {
    const nome = "Enzo";
    console.log(nome);
})();
const nome = "Leal";

console.log(nome);